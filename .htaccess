
# BEGIN WordPress
# Die Anweisungen (Zeilen) zwischen „BEGIN WordPress“ und „END WordPress“ sind
# dynamisch generiert und sollten nur über WordPress-Filter geändert werden.
# Alle Änderungen an den Anweisungen zwischen diesen Markierungen werden überschrieben.
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
RewriteBase /eyadalkhateeb/
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /eyadalkhateeb/index.php [L]
</IfModule>

# END WordPress
# BEGIN Simply Static
# Die Anweisungen (Zeilen) zwischen „BEGIN Simply Static“ und „END Simply Static“ sind
# dynamisch generiert und sollten nur über WordPress-Filter geändert werden.
# Alle Änderungen an den Anweisungen zwischen diesen Markierungen werden überschrieben.
<Files "D:\xampp\htdocs\eyadalkhateeb/wp-content/uploads\simply-static\0b4fd4be394be4e6fb0ea34a3d89632c-debug.txt">
require all denied
require host localhost
</Files>
# END Simply Static